
# First step, loads https://gcaptcha4.geetest.com/load?captcha_id=captcha_id&challenge=d0eb5cd9-1125-4920-a749-a99adb60bfee&client_type=web&lang=es-419&callback=geetest_1789900182542


# callback is defined here:

  var g = "geetest_" + (parseInt(1e4 * o.random()) + new Date().valueOf());

# Challenge here: 

  captcha_id: n.captchaId,
challenge: n.challenge || "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(e) {
    var t = 16 * o.random() | 0;
    return ("x" === e ? t : 3 & t | 8).toString(16)
}),
client_type: i ? "h5" : "web",
risk_type: n.riskType,
call_type: n.callType,
lang: n.language ? n.language : "Netscape" === navigator.appName ? navigator.language.toLowerCase() : navigator.userLanguage.toLowerCase()


        

# captcha_id is provided by the web / backend that uses, with a request like:
POST /api/1/gt4_prep HTTP/1.1

Accept: application/json
Content-Type: application/json
{"c":"l"}

That answers:
{
    "payload": {
        "success": 1,
        "gt": "captcha_id"
    },
    "requestId": "b560685a-35ba-45bd-af24-03cd2d2113f7",
    "access": true
}



# The load returns:
lot_number
payload
process_token


# Then a pow_msg is generated like this:

1|0|md5|2026-09-20 18:38:00.267384+08:00|captcha_id|f73af7dc826f497d9896cf1e29b91f75||9856c6c591802839

# Concatenating a datetime generated:

This looks like a Hashcash-style proof-of-work challenge object! The structure is very recognizable.

Breaking down each field:

Field	Value	Meaning
version	"1"	Hashcash v1 format
bits	0	Number of leading zero bits required in the hash
datetime	"2026-09-20 18:46:39..."	Timestamp / challenge freshness
hashfunc	"md5"	Hash algorithm to use

How Hashcash v1 works:

You construct a string like:

version:bits:datetime:resource:extension:rand:counter

Defined as:
_ᕾᖀᕸᕴ[_ᕵᕴᖆᖆ(762)] = {"version":"1","bits":0,"datetime":"2026-09-20 18:46:39.082902+08:00","hashfunc":"md5"}['datetime']


This is generated by the response server in loading request value "pow_detail": {

# with captcha_id, lot_number, the last value hex is generated here: 

function e() {
            var _ᖈᖈᖄᖙ = _ᕺᖄᖃᖚ.$_DQ()[4][14];
            for (; _ᖈᖈᖄᖙ !== _ᕺᖄᖃᖚ.$_DQ()[0][13]; ) {
                switch (_ᖈᖈᖄᖙ) {
                case _ᕺᖄᖃᖚ.$_DQ()[8][14]:
                    return (65536 * (1 + Math[_ᖂᖀᖈᕷ(146)]()) | 0)[_ᖂᖀᖈᕷ(114)](16)[_ᕵᕴᖆᖆ(76)](1);
                    break
                }
            }
        }

That equals to:
// Deobfuscated equivalent:
function e() {
    return (65536 * (1 + Math.random()) | 0).toString(16).slice(1);
}

Concatenated like this:
e() + e() + e() + e()




# Solution code

Seems to be $_BCEk 

# First important step
for (var i in _ᖀᖈᖂᖙ)
                        if (_ᖀᖈᖂᖙ[_ᖀᕷᖂᖚ(70)](i)) {
                            var r = _ᖀᖈᖂᖙ[i];
                            _ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(517)][_ᖀᕷᖂᖚ(737)] = (0,
                            _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(120)])(i),
                            _ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(517)][_ᖀᕷᖂᖚ(719)] = (0,
                            _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(120)])(r)
                        }

                        being _ᖀᖈᖂᖙ = {
    "(n[16:19])+.+(n[2:5]+n[20:23])+.+(n[28:28]+n[18:18]+n[9:9]+n[13:13])": "n[22:25]"
} (READED Direct from the script source)

r = "n[22:25]"



_ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(517)][_ᖀᕷᖂᖚ(737)] = array of different levels of _ᖀᖈᖂᖙ, something like {"$_BJK":[{"$_BJK":[{"$_BJK":[16,19]}]},{"$_BJK":[{"$_BJK":[2,5]},{"$_BJK":[20,23]}]},{"$_BJK":[{"$_BJK":[28,28]},{"$_BJK":[18,18]},{"$_BJK":[9,9]},{"$_BJK":[13,13]}]}]}


And

_ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(517)][_ᖀᕷᖂᖚ(719)] = {"$_BJK":[{"$_BJK":[{"$_BJK":[22,25]}]}]}



After that:
    _ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(703)] = window[_ᖀᕷᖂᖚ(703)] || _ᕵᖈᖆᖈ[_ᖀᕷᖂᖚ(703)] = window["extraData"] || _ᕵᖈᖆᖈ["extraData"]
                    var _ᕿᖄᖙᕴ = _ᕵᖈᖆᖈ["options"] (Response from the load script, );


# pow_sign / powSign is generated here:
switch (_ᖀᖚᖄᖙ) {
                            case _ᖀᕷᖂᖚ(899): (md5)
                                p = (new (_ᕵᖈᖆᖈ[_ᖀᕷᖂᖚ(68)][_ᖄᖘᕺᖚ(884)]))[_ᖄᖘᕺᖚ(635)](l);
                                break;
                            case _ᖄᖘᕺᖚ(820): (sha1)
                                p = (new (_ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(68)][_ᖀᕷᖂᖚ(832)]))[_ᖄᖘᕺᖚ(635)](l);
                                break;
                            case _ᖀᕷᖂᖚ(827): (sha256)
                                p = (new (_ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(68)][_ᖀᕷᖂᖚ(861)]))[_ᖀᕷᖂᖚ(635)](l)
                            }

Reading the type of hash algorithm to be used from the response of load request pow_detail hashfunc, and generating the hash from powmessage:
'1|0|md5|2026-09-20 19:39:53.056357+08:00|captcha_id|0ed63117f20248e4a0ba247c4f522bd3||7d82422eae516f74' = f1153ce27445a55303097a0b8d00403e






The options object is generated by something similar to this:
// ============================================================
// 1) From the initGeetest4(config, callback) call itself
// ============================================================
options.captchaId = config.captchaId;
options.https     = config.https;
options.product   = config.product;              // "bind"
options.bgColor   = config.bgColor;
options.protocol  = config.https ? "https://" : (config.protocol || location.protocol + "//");

// ============================================================
// 2) From GET /load's JSONP response (load_res.data), each key
//    snake_case -> camelCase via a recursive rename (gt4-loader.js)
// ============================================================
options.lotNumber        = load_res.data.lot_number;
options.captchaType      = load_res.data.captcha_type;        // "ai"
options.js                = load_res.data.js;                  // "/js/gcaptcha4.js"
options.css               = load_res.data.css;
options.staticPath        = load_res.data.static_path;         // "/v4/static/v1.9.7-fc2ddc"
options.gctPath            = load_res.data.gct_path;
options.showVoice          = load_res.data.show_voice;
options.feedback           = load_res.data.feedback;
options.logo               = load_res.data.logo;
options.pt                 = load_res.data.pt;
options.captchaMode        = load_res.data.captcha_mode;       // "adaptive"
options.guard              = load_res.data.guard;
options.checkDevice        = load_res.data.check_device;
options.language           = config.language || load_res.data.language;   // config wins if set
options.langReverse        = load_res.data.lang_reverse;
options.customTheme        = load_res.data.custom_theme;
options.customTheme._brightness = config.custom_theme?._brightness ?? options.customTheme._brightness; // config overrides just this one field
options.payload            = load_res.data.payload;             // opaque blob, generated server-side
options.processToken       = load_res.data.process_token;
options.payloadProtocol    = load_res.data.payload_protocol;    // 1
options.hash                = load_res.data.hash;
options.outside             = load_res.data.outside;
options.hideBindSuccess     = load_res.data.hide_bind_success;
options.hideSuccess         = load_res.data.hide_success;
options.clientType          = load_res.data.client_type;        // "web" (echoes what /load was called with)
options.animate             = load_res.data.animate;
options.powDetail           = load_res.data.pow_detail;         // {version, bits, datetime, hashfunc}

// ============================================================
// 3) Locally computed — this is the part actually worth reading
// ============================================================

// -- deviceId: NOT a real fingerprint call in this path, just reset --
options.deviceId = "";

// -- resetType: cleared every init --
options.resetType = "";

// -- powMsg / powSign: solve the hashcash-style challenge described
//    by powDetail. Call-site argument order (confirmed from source,
//    matches your traced real value field-for-field):
const { pow_msg, pow_sign } = solvePow(
  options.lotNumber,          // p1
  options.captchaId,          // p2
  options.powDetail.hashfunc, // p3  e.g. "md5"
  options.powDetail.version,  // p4  "1"
  options.powDetail.bits,     // p5  0 => any hash instantly qualifies, no brute force needed
  options.powDetail.datetime, // p6
  "",                         // p7  always empty in this flow
);
options.powMsg  = pow_msg;   // `${version}|${bits}|${hashfunc}|${datetime}|${captchaId}|${lotNumber}||` + nonce
options.powSign = pow_sign;  // hash(powMsg) using powDetail.hashfunc

function solvePow(lotNumber, captchaId, hashfunc, version, bits, datetime, extra) {
  const zeroNibbles = bits >> 2, remBits = bits & 3;
  const target = "0".repeat(zeroNibbles);
  const base = [version, bits, hashfunc, datetime, captchaId, lotNumber, extra, ""].join("|");
  for (;;) {
    const nonce = randHex4() + randHex4() + randHex4() + randHex4(); // 16 hex chars
    const msg = base + nonce;
    const digest = hash(hashfunc, msg);
    if (bitsOk(digest, zeroNibbles, remBits)) return { pow_msg: msg, pow_sign: digest };
  }
}

// -- lot / lotRes: NOT computed from performance.timing (my earlier guess
//    on this, before the decoder was extracted, was wrong) — parsed from a
//    single static formula table shipped in the SDK (window.lib._abo):
const FORMULA_TABLE = {
  "(n[16:19])+.+(n[2:5]+n[20:23])+.+(n[28:28]+n[18:18]+n[9:9]+n[13:13])": "n[22:25]",
};
for (const formulaKey in FORMULA_TABLE) {
  options.lot    = parseLotString(formulaKey);              // slice-AST, e.g. {"$_BJK":[...]}
  options.lotRes = parseLotString(FORMULA_TABLE[formulaKey]); // slice-AST for the simpler formula
}
// Only actually *applied* later, inside $_BCEk's verify-payload build step:
//   keyPath = getStringByIndexes(options.lot, options.lotNumber).split(".")
//   value   = getStringByIndexes(options.lotRes, options.lotNumber)
//   merge {[keyPath[0]]: {[keyPath[1]]: ... value}} into the /verify payload
// i.e. the server hands you a random lotNumber, the SDK slices fixed
// character ranges out of it per a hardcoded formula, and the resulting
// nested key/value gets folded into the payload the server hashes-checks —
// proof you ran the real, unmodified SDK against the real lotNumber it issued.







# On other hand the script gct4.5a2e755576738ba0499d714db4f1c9e0.js that is providedd by the initial load

# Generates a unique biht property to add to the object:
n = {
                                geetest: _ᖈᖈᖄᖙ(405),
                                lang: _ᖀᕷᖂᖚ(183),
                                ep: _ᖀᕷᖂᖚ(674)
                            };


In this case with value 1426265548

How is it generated?

The function :

function Fmhz(t) {
                    var cwl = AdJTB.EoY()[0][7];
                    for (; cwl !== AdJTB.EoY()[3][6]; ) {
                        switch (cwl) {
                        case AdJTB.EoY()[3][7]:
                            var e = 5381;
                            var n = t[TioX(17)];
                            var o = 0;
                            while (n--) {
                                e = (e << 5) + e + t[UBkT(26)](o++);
                            }
                            e &= ~(1 << 31);
                            return e;
                            break;
                        }
                    }
                }



Is the hashing djb2


The main function is:

function GErG(t) {
    var dkV = AdJTB.EoY()[3][7];
    for (; dkV !== AdJTB.EoY()[3][6]; ) {
        switch (dkV) {
        case AdJTB.EoY()[0][7]:
            if (t[UBkT(84)] && t[UBkT(34)]) {
                t[e] = Fmhz(GErG[TioX(50)]() + Fmhz(Fmhz[UBkT(50)]())) + UBkT(78);
            }
            return Fmhz(Fmhz[TioX(50)]());
            break;
        }
    }
}




It calls djb2(GErG["toString"]() + djb2(djb2["toString"]))


Finally, seems to be resolved in:

function u(_ᖈᖈᖄᖙ, _ᖀᖚᖄᖙ, _ᕾᖀᕸᕴ, _ᕵᖈᖆᖈ) {
    var _ᖀᖈᖂᖙ = _ᕺᖄᖃᖚ.$_DQ()[0][14];
    for (; _ᖀᖈᖂᖙ !== _ᕺᖄᖃᖚ.$_DQ()[0][12]; ) {
        switch (_ᖀᖈᖂᖙ) {
        case _ᕺᖄᖃᖚ.$_DQ()[8][14]:
            (0,
            _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(173)])(_ᖈᖈᖄᖙ, {
                gee_guard: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(757)]
            }),
            (0,
            _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(173)])(_ᖈᖈᖄᖙ, window[_ᖀᕷᖂᖚ(93)] ? window[_ᖀᕷᖂᖚ(93)] : {});
            var i = (0,
            _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(134)])(_ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(737)], _ᖀᖚᖄᖙ[_ᖀᕷᖂᖚ(520)])
                , r = (0,
            _ᖗᕴᖄᖉ[_ᖄᖘᕺᖚ(134)])(_ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(719)], _ᖀᖚᖄᖙ[_ᖀᕷᖂᖚ(520)])
                , o = i[_ᖀᕷᖂᖚ(111)](_ᖄᖘᕺᖚ(144))
                , a = {};
            o[_ᖄᖘᕺᖚ(779)](function(_ᖂᖀᖈᕷ, _ᖈᖈᖄᖙ, _ᕵᕴᖆᖆ) {
                var _ᖀᕷᖂᖚ = _ᕺᖄᖃᖚ.$_Cr
                    , _ᖀᖚᖄᖙ = ["$_CDCJO"].concat(_ᖀᕷᖂᖚ)
                    , _ᖄᖘᕺᖚ = _ᖀᖚᖄᖙ[1];
                _ᖀᖚᖄᖙ.shift();
                var _ᕾᖀᕸᕴ = _ᖀᖚᖄᖙ[0];
                return _ᕵᕴᖆᖆ === o[_ᖄᖘᕺᖚ(112)] - 1 ? _ᖂᖀᖈᕷ[_ᖈᖈᖄᖙ] = r : _ᖂᖀᖈᕷ[_ᖈᖈᖄᖙ] || (_ᖂᖀᖈᕷ[_ᖈᖈᖄᖙ] = {}),
                _ᖂᖀᖈᕷ[_ᖈᖈᖄᖙ]
            }, a),
            (0,
            _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(173)])(_ᖈᖈᖄᖙ, a),
            _ᖈᖈᖄᖙ[_ᖀᕷᖂᖚ(744)] = {},
            (0,
            _ᕷᕹᕺᖚ[_ᖄᖘᕺᖚ(68)])([], _ᖈᖈᖄᖙ[_ᖄᖘᕺᖚ(744)]);
            _ᖀᖈᖂᖙ = _ᕺᖄᖃᖚ.$_DQ()[4][13];
            break;
        case _ᕺᖄᖃᖚ.$_DQ()[4][13]:
            var _ᖂᖀᖈᕷ = (0,
            _ᖉᕾᖗᖘ[_ᖀᕷᖂᖚ(68)])(_ᖈᖈᖄᖙ, _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(520)])
                , c = (0,
            _ᕸᕹᕺᖚ[_ᖄᖘᕺᖚ(68)])(_ᖁᖂᖂᖚ[_ᖄᖘᕺᖚ(68)][_ᖀᕷᖂᖚ(515)](_ᖈᖈᖄᖙ), _ᕵᖈᖆᖈ)
                , _ = {
                callback: _ᖄᖘᕺᖚ(39),
                captcha_id: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(671)],
                challenge: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(710)],
                client_type: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(795)],
                lot_number: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(520)],
                risk_type: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(709)],
                payload: _ᖀᖚᖄᖙ[_ᖀᕷᖂᖚ(750)],
                process_token: _ᖀᖚᖄᖙ[_ᖀᕷᖂᖚ(730)],
                payload_protocol: _ᖀᖚᖄᖙ[_ᖀᕷᖂᖚ(772)],
                pt: _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(786)],
                w: c
            };
            _ᖂᖀᖈᕷ && (_[_ᖀᕷᖂᖚ(742)] = _ᖂᖀᖈᕷ),
            (_ᕵᖈᖆᖈ[_ᖀᕷᖂᖚ(703)] && _ᖄᖘᕺᖚ(721) === _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(795)] || _ᖀᕷᖂᖚ(756) === _ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(795)] && !_ᖀᖚᖄᖙ[_ᖀᕷᖂᖚ(434)]) && (_[_ᖄᖘᕺᖚ(716)] = _ᕵᖈᖆᖈ[_ᖀᕷᖂᖚ(703)] && _ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(703)][_ᖄᖘᕺᖚ(716)] ? _ᕵᖈᖆᖈ[_ᖄᖘᕺᖚ(703)][_ᖄᖘᕺᖚ(716)] : null),
            !_ᖀᖚᖄᖙ[_ᖄᖘᕺᖚ(718)] && _[_ᖄᖘᕺᖚ(716)] && delete _[_ᖄᖘᕺᖚ(716)],
            (0,
            _ᖄᖄᖗᖈ[_ᖄᖘᕺᖚ(470)])(_ᖀᖚᖄᖙ, _ᖄᖘᕺᖚ(770), _, _ᕿᖗᖗᕵ)[_ᖄᖘᕺᖚ(404)](function(_ᖂᖀᖈᕷ) {
                var _ᖈᖈᖄᖙ = _ᕺᖄᖃᖚ.$_Cr
                    , _ᖀᕷᖂᖚ = ["$_CDDEx"].concat(_ᖈᖈᖄᖙ)
                    , _ᖀᖚᖄᖙ = _ᖀᕷᖂᖚ[1];
                _ᖀᕷᖂᖚ.shift();
                var _ᖄᖘᕺᖚ = _ᖀᕷᖂᖚ[0];
                var _ᖀᖈᖂᖙ = _ᕵᖈᖆᖈ[_ᖀᖚᖄᖙ(767)](_ᖂᖀᖈᕷ);
                if (_ᖀᖚᖄᖙ(331) === _ᖀᖈᖂᖙ[_ᖈᖈᖄᖙ(611)])
                    return (0,
                    _ᖁᕺᖗᖘ[_ᖀᖚᖄᖙ(535)])((0,
                    _ᖁᕺᖗᖘ[_ᖈᖈᖄᖙ(526)])(_ᖂᖀᖈᕷ, _ᕵᖈᖆᖈ, _ᖀᖚᖄᖙ(729)));
                _ᕵᕴᖆᖆ ? _ᕾᖀᕸᕴ(_ᖀᖈᖂᖙ[_ᖀᖚᖄᖙ(752)]) : _ᕵᖈᖆᖈ[_ᖀᖚᖄᖙ(782)](_ᖀᖈᖂᖙ[_ᖀᖚᖄᖙ(752)], _ᕾᖀᕸᕴ)
            }, function() {
                var _ᖂᖀᖈᕷ = _ᕺᖄᖃᖚ.$_Cr
                    , _ᖈᖈᖄᖙ = ["$_CDDJB"].concat(_ᖂᖀᖈᕷ)
                    , _ᕵᕴᖆᖆ = _ᖈᖈᖄᖙ[1];
                _ᖈᖈᖄᖙ.shift();
                var _ᖀᕷᖂᖚ = _ᖈᖈᖄᖙ[0];
                return (0,
                _ᖁᕺᖗᖘ[_ᕵᕴᖆᖆ(535)])((0,
                _ᖁᕺᖗᖘ[_ᖂᖀᖈᕷ(536)])(_ᕵᕴᖆᖆ(792), _ᕵᖈᖆᖈ))
            });
            _ᖀᖈᖂᖙ = _ᕺᖄᖃᖚ.$_DQ()[4][12];
            break
        }
    }
}


That calls i to generate a ciphering of the w component:
function i(_ᖈᖈᖄᖙ, _ᕵᕴᖆᖆ) {
    var _ᖀᖚᖄᖙ = _ᕺᖄᖃᖚ.$_DQ()[8][14];
    for (; _ᖀᖚᖄᖙ !== _ᕺᖄᖃᖚ.$_DQ()[8][12]; ) {
        switch (_ᖀᖚᖄᖙ) {
        case _ᕺᖄᖃᖚ.$_DQ()[0][14]:
            var n = _ᕵᕴᖆᖆ[_ᖀᕷᖂᖚ(517)];
            if (!n[_ᖄᖘᕺᖚ(786)] || _ᖀᕷᖂᖚ(258) === n[_ᖄᖘᕺᖚ(786)])
                return _ᕵᖈᖆᖈ[_ᖀᕷᖂᖚ(68)][_ᖄᖘᕺᖚ(900)](_ᖈᖈᖄᖙ);
            _ᖀᖚᖄᖙ = _ᕺᖄᖃᖚ.$_DQ()[8][13];
            break;
        case _ᕺᖄᖃᖚ.$_DQ()[4][13]:
            var s = (0,
            _ᖄᖄᖗᖈ[_ᖄᖘᕺᖚ(192)])()
                , _ᖂᖀᖈᕷ = new (_ᖄᖄᖗᖈ[_ᖀᕷᖂᖚ(59)])([_ᖄᖘᕺᖚ(846), _ᖄᖘᕺᖚ(952)])
                , r = {
                1: {
                    symmetrical: _ᕿᖄᖙᕴ[_ᖀᕷᖂᖚ(68)],
                    asymmetric: new (_ᕿᖗᖗᕵ[_ᖄᖘᕺᖚ(68)])
                },
                2: {
                    symmetrical: new (_ᖄᕷᕴᖁ[_ᖀᕷᖂᖚ(68)])({
                        key: s,
                        mode: _ᖀᕷᖂᖚ(973),
                        iv: _ᖄᖘᕺᖚ(970)
                    }),
                    asymmetric: _ᖗᕴᖄᖉ[_ᖀᕷᖂᖚ(68)]
                }
            };
            if (_ᖂᖀᖈᕷ[_ᖄᖘᕺᖚ(167)](n[_ᖄᖘᕺᖚ(786)])) {
                var o = _ᖀᕷᖂᖚ(846) === n[_ᖀᕷᖂᖚ(786)]
                    , a = n[_ᖄᖘᕺᖚ(786)]
                    , u = r[a][_ᖄᖘᕺᖚ(968)][_ᖀᕷᖂᖚ(998)](s);
                while (o && (!u || 256 !== u[_ᖀᕷᖂᖚ(112)]))
                    s = (0,
                    _ᖄᖄᖗᖈ[_ᖀᕷᖂᖚ(192)])(),
                    u = (new (_ᕿᖗᖗᕵ[_ᖀᕷᖂᖚ(68)]))[_ᖀᕷᖂᖚ(998)](s);
                var c = r[a][_ᖄᖘᕺᖚ(1077)][_ᖀᕷᖂᖚ(998)](_ᖈᖈᖄᖙ, s);
                return (0,
                _ᖄᖄᖗᖈ[_ᖄᖘᕺᖚ(175)])(c) + u
            }
            _ᖀᖚᖄᖙ = _ᕺᖄᖃᖚ.$_DQ()[8][12];
            break
        }
    }
}

Sending as arguments:
_ᖈᖈᖄᖙ = '{"9986":{"81204373":{"78f3":"73a6"}},"device_id":"","lot_number":"f48120300ff2434699864373a68e785a","pow_msg":"1|0|md5|2026-09-21 01:42:24.230044+08:00|captcha_id|f48120300ff2434699864373a68e785a||a4f256a874d3261b","pow_sign":"859ebe2ab514f9f70bc97741e9f77352","geetest":"captcha","lang":"zh","ep":"123","biht":"1426265548","d4tf":"9342","em":{"ph":0,"cp":0,"ek":"11","wd":1,"nt":0,"si":0,"sc":0}}'

_ᕵᕴᖆᖆ = _ᕵᖈᖆᖈ {lastType: '', isBoxShow: false, options: a, $_BBIf: _ᖄᕷᕴᖁ, $_BBJG: _ᖄᕷᕴᖁ, …} $_BBIf :  _ᖄᕷᕴᖁ {$_DEA: Window} $_BBJG :  _ᖄᕷᕴᖁ {$_DEA: document} $_BCBr :  39 event :  _ᖀᖈᖂᖙ {eventList: Array(0)} extraData :  undefined isBoxShow :  false lastType :  "" options :  a {captchaId: 'captcha_id', https: true, product: 'bind', bgColor: '#00000050', protocol: 'https://', …} status :  s {processor: {…}, ctx: _ᕵᖈᖆᖈ, status: 'init', $_BDBD: '', onChange: ƒ} [[Prototype]] :  Object


We have left to define:

# "em"

Is defined here:
_ᕷᕹᕺᖚ[_ᖄᖘᕺᖚ(68)])([], _ᖈᖈᖄᖙ[_ᖄᖘᕺᖚ(744)]);

Calling to:


return function _ᖂᖀᖈᕷ(_ᖈᖈᖄᖙ, _ᕵᕴᖆᖆ) {
    var _ᖀᕷᖂᖚ = _ᕺᖄᖃᖚ.$_Cr
        , _ᖀᖚᖄᖙ = ["$_HIBJS"].concat(_ᖀᕷᖂᖚ)
        , _ᖄᖘᕺᖚ = _ᖀᖚᖄᖙ[1];
    _ᖀᖚᖄᖙ.shift();
    var _ᕾᖀᕸᕴ = _ᖀᖚᖄᖙ[0];
    for (var n, s, i = C, r = -1, o = i[_ᖀᕷᖂᖚ(112)]; ++r < o; )
        s = (n = i[r])[1](r),
        _ᕵᕴᖆᖆ[n[0]] = s;
    return _ᖈᖈᖄᖙ
}

Basically is (Seems totally easy to statically craft):

function computeEm() {
  const has = (key, obj) => key in obj;

  const em = {

    // "_phantom" in window          -> PhantomJS
    // 0 = absent, 1 = present
    ph: has("_phantom", window) ? 1 : 0,

    // "callPhantom" in window, then try reading it -> PhantomJS (older builds)
    // 0 = absent, 1 = present & readable, 9 = present but throws on access
    cp: (() => {
      if (!has("callPhantom", window)) return 0;
      try { window.callPhantom; } catch (e) { return 9; }
      return 1;
    })(),

    // Engine/error-shape fingerprint: which props exist on a thrown TypeError
    // differs by JS engine (V8 vs SpiderMonkey vs old IE/JScript, headless shells, etc)
    // value = 10-bit presence mask, hex-encoded (e.g. "11" = 0b0000010001)
    ek: (() => {
      const props = [
        "line", "column", "lineNumber", "columnNumber",
        "fileName", "message", "number", "description",
        "sourceURL", "stack",
      ];
      let err;
      try { (5 * Math.random())(); } catch (e) { err = e; } // call a number -> throws
      const bits = props.map(p => (has(p, err) ? 1 : 0)).join("");
      return parseInt(bits, 2).toString(16);
    })(),

    // navigator.webdriver, walking the prototype chain instead of a naive truthy check
    // 0 = absent, 1 = present & false, 2 = present & true,
    // 8 = no descriptor owner found, 9 = descriptor exists but isn't a plain object
    wd: (() => {
      const key = "webdriver";
      const proto = Object.getPrototypeOf ? Object.getPrototypeOf(navigator) : navigator.__proto__;
      if (!proto) return 8;
      if (!has(key, proto)) {
        return has(key, navigator) ? (navigator[key] ? 2 : 1) : 0;
      }
      if (!Object.getOwnPropertyDescriptor) {
        return navigator[key] ? 2 : 1;
      }
      const desc = Object.getOwnPropertyDescriptor(proto, key);
      if (typeof desc !== "object") return 9;
      const val = desc.get ? desc.get.call(navigator) : desc.value;
      return val ? 2 : 1;
    })(),

    // "__nightmare" in window       -> Nightmare.js headless browser
    // 0 = absent, 1 = present
    nt: has("__nightmare", window) ? 1 : 0,

    // "_webdriver_script_fn" in document -> Selenium-injected DOM property
    // 0 = absent, 1 = present
    si: has("_webdriver_script_fn", document) ? 1 : 0,

    // "$cdc_asdjflasutopfhvcZLmcfl_" in document -> ChromeDriver's randomized CDC marker
    // 0 = absent, 1 = present
    sc: has("$cdc_asdjflasutopfhvcZLmcfl_", document) ? 1 : 0,
  };

  return em;
}

# "9986":{"81204373":{"78f3":"73a6"}}

// The formula table is shipped hardcoded in gcaptcha4.js's own bootstrap IIFE
// as window.lib._abo = { "<keyFormula>": "<valueFormula>" }.
const LOT_FORMULA_TABLE = {
  "(n[16:19])+.+(n[2:5]+n[20:23])+.+(n[28:28]+n[18:18]+n[9:9]+n[13:13])": "n[22:25]",
};

// Parses one formula string, e.g. "(n[16:19])+.+(n[2:5]+n[20:23])+.+(...)",
// into segments: each "+.+ "-separated group becomes one array of [start,end)
// slice ranges (already resolved to plain JS .slice() bounds).
function parseLotFormula(formula) {
  const parseIndices = (bracketContent) => {
    // "16:19" -> [16,19]; "28:28" -> [28,28]; single "5" -> [5]
    const nums = bracketContent.split(":").map((n) => parseInt(n.trim(), 10));
    const start = nums[0];
    const end = nums.length > 1 ? nums[1] + 1 : nums[0] + 1; // end is inclusive in the formula
    return [start, end];
  };

  const parseAtom = (atom) => {
    // atom looks like "n[16:19]" -> pull out the "16:19" part
    const match = atom.match(/\[(.*?)\]/)[1];
    return parseIndices(match);
  };

  // top-level groups are separated by "+.+ " -> become the "." joins later
  return formula.split("+.+").map((group) => {
    const cleaned = group.replace(/[()]/g, ""); // strip the wrapping parens
    // a group can itself concatenate several slices with "+", e.g. "n[2:5]+n[20:23]"
    return cleaned.split("+").map(parseAtom);
  });
}

// Applies a parsed formula (array of [start,end] groups) to the real lot_number,
// slicing each range out and joining: atoms within a group with "", groups with "."
function applyLotFormula(parsedFormula, lotNumber) {
  return parsedFormula
    .map((group) => group.map(([start, end]) => lotNumber.slice(start, end)).join(""))
    .join(".");
}

// Builds the nested {[k1]: {[k2]: {[k3]: value}}} proof object the server expects,
// proving the SDK ran the real formula table against the real lot_number it issued.
function buildLotProof(lotNumber, formulaTable = LOT_FORMULA_TABLE) {
  const result = {};
  for (const keyFormula in formulaTable) {
    const valueFormula = formulaTable[keyFormula];

    const keyPath = applyLotFormula(parseLotFormula(keyFormula), lotNumber).split(".");
    const value = applyLotFormula(parseLotFormula(valueFormula), lotNumber);

    let cursor = result;
    keyPath.forEach((key, i) => {
      if (i === keyPath.length - 1) {
        cursor[key] = value;
      } else {
        cursor[key] = cursor[key] || {};
        cursor = cursor[key];
      }
    });
  }
  return result;
}

// --- example, using the real captured lot_number ---
const lotNumber = "f48120300ff2434699864373a68e785a";
console.log(JSON.stringify(buildLotProof(lotNumber)));


# d4tf: "9342" 
Hardcoded at the beginning, at d4tf: _ᕵᕴᖆᖆ(45) = "9342"

# Ciphering

First generates guid like this:

// Deobfuscated equivalent:
function e() {
    return (65536 * (1 + Math.random()) | 0).toString(16).slice(1);
}

Concatenated like this:
e() + e() + e() + e()


and saves it into s variable (This is the symmetric key)

Then ciphers inside variable u the symmetric key with an assymetric algorithm:
u = r[a]["asymmetric"]["encrypt"](s);



After that, encrypts :
_ᖈᖈᖄᖙ = '{"device_id":"","lot_number":"828c56c41bb940839e7269e2f4e3050d","pow_msg":"1|0|md5|2026-09-21 02:11:20.428360+08:00|captcha_id|828c56c41bb940839e7269e2f4e3050d||3b5d803ed5be831d","pow_sign":"830d88571558ffd37c4ff0427195773d","geetest":"captcha","lang":"zh","ep":"123","biht":"1426265548","d4tf":"9342","9e72":{"8c5669e2":{"07b0":"e2f4"}},"em":{"ph":0,"cp":0,"ek":"11","wd":1,"nt":0,"si":0,"sc":0}}'

With the symmetric key:

r[a]["symmetrical"]["encrypt"](_ᖈᖈᖄᖙ, s);

And returns the ciphered:
return (0, _ᖄᖄᖗᖈ["arrayToHex"])(c) + u;

With the encrypted key concatenated as u variable

It uses RSA 1024:

function _ᖁᕺᖗᖘ() {
    var _ᖈᖈᖄᖙ = _ᕺᖄᖃᖚ.$_DQ()[4][14];
    for (; _ᖈᖈᖄᖙ !== _ᕺᖄᖃᖚ.$_DQ()[4][13]; ) {
    switch (_ᖈᖈᖄᖙ) {
        case _ᕺᖄᖃᖚ.$_DQ()[0][14]:
        ((this["n"] = null),
            (this["e"] = 0),
            (this["d"] = null),
            (this["p"] = null),
            (this["q"] = null),
            (this["dmp1"] = null),
            (this["dmq1"] = null),
            (this["coeff"] = null));
        this["setPublic"](
            "",
            "10001",
        );
        _ᖈᖈᖄᖙ = _ᕺᖄᖃᖚ.$_DQ()[4][13];
        break;
    }
    }
}

# + Symmetric SM4 (SMS4)



Full pseudocode:

// Assume imported: node-forge (RSA) + sm-crypto (SM4) — both real, widely-used npm packages
import forge from "node-forge";
import { sm4 } from "sm-crypto";

// Hardcoded public key extracted from gcaptcha4.js (1024-bit modulus, e=65537)
const RSA_MODULUS_HEX  = "";
const RSA_EXPONENT_HEX = "10001"; // 65537

function rsaPublicKey() {
  const n = new forge.jsbn.BigInteger(RSA_MODULUS_HEX, 16);
  const e = new forge.jsbn.BigInteger(RSA_EXPONENT_HEX, 16);
  return forge.pki.setRsaPublicKey(n, e);
}

// Exact SDK algorithm: 4x "4 random hex chars", concatenated -> 16 ASCII chars = 16 bytes.
// Uses Math.random(), not a CSPRNG — replicated as-is for wire compatibility.
function e4() {
  return (65536 * (1 + Math.random()) | 0).toString(16).slice(1);
}
function randomSm4Key() {
  return e4() + e4() + e4() + e4(); // 16-char ASCII string, used as raw key bytes
}

// RSA-PKCS1v1.5-encrypt the symmetric key -> hex ("u"). 1024-bit modulus -> 256 hex chars
// once you're not unlucky with a leading zero byte (hence the retry loop below).
function rsaEncryptKeyHex(key16) {
  const encrypted = rsaPublicKey().encrypt(key16, "RSAES-PKCS1-V1_5");
  return forge.util.bytesToHex(encrypted);
}

// SM4-CBC-encrypt the JSON payload with the same 16-byte key. Zero IV, PKCS7 padding.
function sm4EncryptHex(jsonString, key16) {
  const keyHex = forge.util.bytesToHex(key16);
  const ivHex  = "30303030303030303030303030303030"; // hex for 16 ascii '0' chars
  return sm4.encrypt(jsonString, keyHex, { mode: "cbc", iv: ivHex, padding: "pkcs#7", output: "string" });
}

// Full pipeline — matches the traced flow exactly:
//   s = guid(); u = RSA.encrypt(s); c = SM4.encrypt(payload, s); return hex(c) + u
function buildW(payload, pt) {
  const data = JSON.stringify(payload);

  let key = randomSm4Key();
  let u = rsaEncryptKeyHex(key);

  if (pt === "1") {
    // real SDK retries only in this branch, until the RSA blob is exactly 256 hex chars
    while (!u || u.length !== 256) {
      key = randomSm4Key();
      u = rsaEncryptKeyHex(key);
    }
  }

  const c = sm4EncryptHex(data, key);
  return c + u; // final `w` param sent to /verify
}

            

The value w= seems to be the solution to the challenge, a hex



$_BCEk final step

# RESOLVED: protocol 1 ("pt":"1", the one this captcha_id actually issues) is AES, not SM4

The SM4 pseudocode above was the right shape but the wrong cipher for `pt`
"1" — confirmed by live `/verify` calls returning `-50002 param decrypt
error` for every SM4 variant tried (CBC/iv=key, CBC/iv=zero, ECB). Root
cause: `r[a]["symmetrical"]` in the dispatch function (the `i()` shown
above, `sources/gcaptcha4_deobfuscated.js:11507-11549`) is only the SM4
class for `pt "2"`. For `pt "1"` it's `_ᕿᖄᖙᕴ["default"]`, used as-is with no
construction and no mode/iv ever passed at the call site.

Tracked `_ᕿᖄᖙᕴ` down to its actual module by byte-offset alignment in the
raw (non-deobfuscated) bundle: the dispatch site's `require(33..37)` calls
sit right next to three back-to-back modules whose *content* pins them
unambiguously —
`sources/gcaptcha4_deobfuscated.js:12919` contains the exact hardcoded RSA
modulus hex from this same doc (confirms the module right after `_ᕿᖄᖙᕴ`'s is
the RSA class), and `sources/gcaptcha4_deobfuscated.js:13650-13955` is the
already-confirmed hand-rolled SM4-CBC class (confirms the module *two*
after `_ᕿᖄᖙᕴ`'s). That pins `_ᕿᖄᖙᕴ`'s own module to
`sources/gcaptcha4_deobfuscated.js:11899-12549` — which is verbatim,
unmodified **CryptoJS** (`Base`/`WordArray`/`BufferedBlockAlgorithm`/
`Cipher`/`_createHelper`), ending in `t["AES"] = ...`.

The decisive bit is CryptoJS's own `_createHelper`-equivalent
(`sources/gcaptcha4_deobfuscated.js:12163-12194`):

```js
encrypt: function (message, key, cfg) {
  key = Latin1.parse(key);                 // raw bytes, NOT an OpenSSL/KDF passphrase
  (cfg && cfg.iv) || ((cfg = cfg || {}).iv = Latin1.parse("0000000000000000"));
  var s = SerializableCipher.encrypt(AES, message, key, cfg);
  // ...flatten s.ciphertext into a plain byte array and return it (IV NOT prepended)...
}
```

The dispatch call passes only `(data, s)` — no `cfg` — so `cfg.iv` always
falls through to `"0000000000000000"`, the **same 16-ASCII-zero-char IV**
`pt "2"`'s SM4 uses. Mode/padding are CryptoJS's own untouched defaults:
CBC + Pkcs7. So: **`pt "1"` = AES-128-CBC, zero IV, 16 raw guid bytes as
the key, PKCS7 padding, output = ciphertext bytes only (hex) + RSA(key)
hex** — structurally identical pipeline to the SM4 doc above, just a
different symmetric cipher.

Implemented in `solve/08_encrypt_payload.js` (`buildW`) and confirmed live:
`node solve/09_send_verify.js` after running steps 01-08 returns
`{"status":"success", "data":{"result":"success", "seccode":{"pass_token":...}}}`
from the real `gcaptcha4.geetest.com/verify` endpoint.